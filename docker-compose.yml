services:
  prizm-api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - MAX_BATCH_POSTAL_CODES=10
    volumes:
      - ./templates:/app/templates
      - ./prizm_cache_v2.db:/app/prizm_cache_v2.db
    restart: unless-stopped
